window.onload = function() {
	// Custom JS
	const typed = new Typed('#typed', {
		stringsElement: '#typed-strings',
		smartTyping: true,
		typeSpeed: 50,
		backSpeed: 25,
		// fadeOut: true,
		loop: true
	})
}
