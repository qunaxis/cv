window.onload = function() {
	// Custom JS
	const typed = new Typed('.cta__description', {
		stringsElement: '#typed-strings',
		smartTyping: true,
		typeSpeed: 50,
		backSpeed: 25,
		// fadeOut: true,
		loop: true
	})
}
